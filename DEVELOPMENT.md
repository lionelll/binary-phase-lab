# 本地开发与授权边界

## 本地流程

1. 使用 `npm install` 或 `npm ci` 安装锁定依赖。
2. 使用 `npm run dev` 启动本地开发页面。
3. 修改完成后必须运行 `npm test` 与 `npm run build`。
4. 涉及相界、相区或不变量数据的修改，应同步更新单元测试和 `docs/data-sources.md`。

## 项目边界

- 本项目只允许修改 `材科基二元相图部分/binary-phase-lab/`。
- 晶体结构、三元相图和入口页只可作为只读参考。
- 测试部署基路径为 `/binary/`；不得将测试配置主动改向正式环境。
- Git 分支、提交、推送、测试部署、正式发布和版本标签流程尚未制定，所有外部操作均需产品所有者另行明确授权。
