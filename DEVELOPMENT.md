# 本地开发与授权边界

## 本地流程

1. 使用 `npm install` 或 `npm ci` 安装锁定依赖。
2. 使用 `npm run dev` 启动本地开发页面。
3. 修改完成后必须运行 `npm test` 与 `npm run build`。
4. 涉及相界、相区或不变量数据的修改，应同步更新单元测试和 `docs/data-sources.md`。

## 分支与发布流程

- `dev`：唯一的日常开发分支。所有功能开发、缺陷修复和文档更新均先提交到此分支。
- `release`：发布分支。只接收已经在 `dev` 完成并通过测试、构建验证的版本。
- `main`：保留稳定基线，不直接进行日常开发；是否同步发布版本需由产品所有者明确授权。
- 发布前必须在 `dev` 运行 `npm test` 与 `npm run build`，通过后再将确定的版本合并到 `release`。
- 部署、正式发布和版本标签仍需产品所有者另行授权。

## 项目边界

- 本项目只允许修改 `材科基二元相图部分/binary-phase-lab/`。
- 晶体结构、三元相图和入口页只可作为只读参考。
- 测试部署基路径为 `/binary/`；不得将测试配置主动改向正式环境。
- 除已明确的 `dev` / `release` 分支流程外，测试部署、正式发布和版本标签等外部操作均需产品所有者另行明确授权。
